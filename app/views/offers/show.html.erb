<%= content_for :title, "Minha oferta" %>


<div class="row">
  <div class="col-md-6 top">
  <h1><%= @offer.title %></h1>
  <p><%= @offer.description %></p>
  <hr>
  <p><%= @offer.price %></p>
    <div class="userinfo"
    <p><%= @offer.user.photo %></p>
    <%= cl_image_tag @offer.user.photo, height: 50, width: 50, class: 'img-user' %>
    <p><%= @offer.user.mobile %></p>
    </div>
    <div class="col-md-8">
      <button type="button" class="botaum" aria-busy="false">
        <span class="_16c9pko4">
          <span>Eu quero</span>
        </span>
      </button>
    </div>
</div>
<div class="col-md-6">
<p><%= @offer.photo %></p>
<%= cl_image_tag @offer.photo, height: 300, width: 400, crop: :fill %>
</div></div>
